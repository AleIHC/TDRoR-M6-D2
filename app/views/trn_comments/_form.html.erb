<%= form_with(model: @trn_comment, url: trn_new_trn_comments_path(@trn_new), method: :post) do |form| %>
  
  <% if trn_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(trn_comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% trn_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row d-flex justify-content-center comment">
      <div class="col-md-8 col-lg-6 mb-5">
        <div class="card shadow-0 border">
          <div class="card-body p-0">
            <div class="card m-0">
              <div class="card-body">
               

              
                  <div class="d-flex flex-row align-items-center">
                    <img src="https://mdbcdn.b-cdn.net/img/Photos/Avatars/img%20(4).webp" alt="avatar" width="100"  height="100"
                      height="25" />
                     <div class="comment-content ms-2">
                        <%= form.text_area :content, class:"comment-content" %>
                      </div>
                  </div>

              </div>
            </div>
            </div>
          </div>

          <div class="text-center mt-2">
            <%= form.submit  "Guardar comentario", class:"btn btn-dark news-title" %>
          </div>

        </div>
      </div>
      
    </div>
    

<% end %>
